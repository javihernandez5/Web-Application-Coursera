##include "algo.h"
int main(int argc, char const *argv[])
{
	int ca;
	int count;
	int * c = (int*)malloc(2*sizeof(int));

	c={1,2};

	
	if (c ==0)
	{
		/* code */

	}
	for (int i = 0; i < count; ++i)
	{
		/* code */

		c++;
	}
	return 0;
}